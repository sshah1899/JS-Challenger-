//Get last n characters of string
//- used for selecting characters from last to begun.

function myFunction(str) {
   
    return str.slice(-3)
 };
 let a = "my name is sunny"
 console.log(a.slice(0,10))// o start from my and ends to s 10.
 console.log(a.slice(-5)) // 0 start from y.

let ab = [1, 2, 3];
console.log(ab)